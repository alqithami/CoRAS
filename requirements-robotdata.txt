# Install after the base requirements when you want public robot datasets.
# macOS/Apple-Silicon note: CoRAS exporters default to LeRobot video_backend=pyav
# to avoid TorchCodec/FFmpeg dylib mismatch errors during one-time NPZ export.
lerobot>=0.5.1,<0.6
huggingface_hub>=0.24
av>=15.0
imageio[ffmpeg]>=2.34
# Optional on Linux/RunPod if you prefer the faster backend and have matching FFmpeg:
# torchcodec==0.10.*  # for torch 2.10.*
